# راهنمای مشارکت

## قبل از تغییر

1. AGENTS.md و سند مرتبط را بخوانید.
2. branch کوتاه و هدفمند بسازید.
3. fixture واقعی حساس را بدون بی‌نام‌سازی وارد Git نکنید.

## توسعه

- business rule ابتدا تست domain می‌گیرد.
- تغییر API با schema و مثال هماهنگ می‌شود.
- تغییر معماری مهم ADR می‌گیرد.
- migration مخرب بدون طرح انتقال پذیرفته نیست.

## کنترل کیفیت

    make test
    make lint

برای فرانت‌اند production build نیز اجرا شود:

    pnpm --dir frontend build

## Pull Request

توضیح باید شامل مسئله، راه‌حل، تست‌های اجراشده، اثر امنیتی و تصاویر UI در صورت تغییر ظاهری باشد.
